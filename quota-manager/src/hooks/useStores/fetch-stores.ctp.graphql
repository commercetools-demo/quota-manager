#import './store-fragment.ctp.graphql'
query FetchStores($limit: Int!, $offset: Int!, $sort: [String!], $where: String, $locale: Locale!) {
  stores(limit: $limit, offset: $offset, sort: $sort, where: $where) {
    total
    count
    offset
    results {
      ...StoreFragment
    }
  }
}
